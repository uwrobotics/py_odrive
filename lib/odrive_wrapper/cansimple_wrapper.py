# axis#_command synethsize
# write an enum for all the commadn
# multiprocessing and worker

# Import DBC-> check DBC wrapper and generic DBC
# Keep the current DBC max support up to 8 device will see next step (autogen)


#config file -> yaml for mapping read into each motor and name